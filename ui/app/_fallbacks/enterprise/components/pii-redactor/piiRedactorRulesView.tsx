// The PII redactor rules UI is now part of the open-source Loopback Gateway
// build. The @enterprise alias resolves here in OSS builds, so we re-export the
// real workspace view instead of an upsell stub.
export { default } from "@/app/workspace/pii-redactor/views/piiRedactorRulesView";